# Public HTTP(S) origin used for canonical, Open Graph, sitemap, and robots URLs.
SITE_URL=https://makestopmotion.com

# Optional contact page and address. The page is hidden when this is empty.
CONTACT_EMAIL=

# Optional Umami analytics. Tracking is only enabled when both values are set.
UMAMI_URL=
UMAMI_WEBSITE_ID=
